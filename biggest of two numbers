include<stdio.h>
int main()
{
int x,y;
printf("enter the x :");
scanf("%d",&x);
printf("enter the y:");
scanf("%d",&y);
if(x>y)
{
printf("x is the biggest number");
}
else
{
printf("y is biggest number");
}
return 0;
}
